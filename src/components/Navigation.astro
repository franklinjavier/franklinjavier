---
import ThemeToggle from './ThemeToggle.astro'
import LanguageToggle from './LanguageToggle.astro'
import { useTranslations, getLangFromUrl } from '../i18n/utils'
import { nav, navInner, navRow, navLink, navActions } from '../styles/app'

const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)

const homeUrl = lang === 'en' ? '/' : '/pt-br/'
const aboutUrl = lang === 'en' ? '/#about' : '/pt-br/#about'
const postsUrl = lang === 'en' ? '/blog/' : '/pt-br/blog/'
const speakingUrl = lang === 'en' ? '/speaking/' : '/pt-br/speaking/'
---

<nav class={nav.class}>
  <div class={navInner.class}>
    <div class={navRow.class}>
      <a href={homeUrl} class={navLink.class}>{t('nav.home')}</a>
      <a href={postsUrl} class={navLink.class}>{t('nav.blog')}</a>
      <a href={speakingUrl} class={navLink.class}>{t('nav.speaking')}</a>
      <a href={aboutUrl} class={navLink.class}>{t('nav.about')}</a>
      <div class={navActions.class}>
        <LanguageToggle />
        <ThemeToggle />
      </div>
    </div>
  </div>
</nav>
