{{> header}}

  <article class="post">

    <h1>{{ title }}</h1>
    <time datetime="{{ dateFormat date }}">{{ dateFormat date }}</time>

    {{> tags}}

    <div class="post__body">
      {{{contents}}}
    </div>

  </article>

{{> footer}}
