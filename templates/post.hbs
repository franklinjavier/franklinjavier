{{> header}}

  <article class="post">

    <h1>{{ title }}</h1>
    <time datetime="{{ dateFormat date }}">{{ dateFormat date }}</time>

    {{> tags}}

    <div class="post-body">
      {{{contents}}}
    </div>

    {{> share}}

  </article>

{{> footer}}
