{{> header}}

  <article class="post">

    {{> tags}}

    <h1>{{ title }}</h1>
    <time datetime="{{ dateFormat date }}">{{ dateFormat date }}</time>

    <div class="post-body">
      {{{contents}}}
    </div>

    {{> share}}

  </article>

{{> footer}}
